<h1>Song List</h1>
<% @songs.each do |song|%>
    <% if song.artist %>
        <p><%= link_to 'song.artist.name - song.title', song_path(song) %></p>
    <% else %>
        <p><%= link_to song.title, song_path(song) %></p>
    <% end %>
<% end %>