<h1>Song List</h1>
<% @songs.each do |song|%>
    <% if song.artist %>
        <%= link_to "#{song.artist.name} - #{song.title}", song_path(song) %><%= link_to song.artist.name, artist_path(song.artist)%>
    <% else %>
        <%= link_to song.title, song_path(song) %>
    <% end %>
<% end %>